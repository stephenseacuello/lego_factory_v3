# Move robot to position service
string robot_id
string move_type          # joints or pose
float64[] target_joints   # For joint moves (6 values)
geometry_msgs/Pose target_pose  # For cartesian moves
float64 velocity_scaling  # 0.0 - 1.0
bool wait
---
bool success
string message
float64 execution_time
