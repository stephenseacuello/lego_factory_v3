# GetSupervisorStatus.srv - Get supervisor tree status
# LEGO MCP Manufacturing System v7.0
# OTP-style supervision monitoring

# Request
string supervisor_id                # Supervisor ID (empty for root)
bool include_children               # Include child status details
bool include_metrics                # Include health metrics
bool recursive                      # Include nested supervisors recursively

---
# Response
bool success                        # True if supervisor found
string message                      # Human-readable result message

# Supervisor status
lego_mcp_msgs/SupervisorStatus status

# Tree-wide metrics (if recursive)
uint32 total_supervisors            # Total supervisors in tree
uint32 total_nodes                  # Total managed nodes
uint32 total_running                # Total running nodes
uint32 total_failed                 # Total failed nodes
float32 overall_health_score        # Overall system health (0.0 - 1.0)
